# Ejemplo de variables de entorno (NO subir secrets reales)
# Ejemplo de variables de entorno (NO subir secrets reales)
# URL de conexión a la base de datos (por ejemplo, SQLite, Postgres, etc.)
DATABASE_URL="file:./dev.db"
# Clave de administrador para operaciones privilegiadas (usar un valor seguro en producción)
ADMIN_KEY="CambiaMe123!"
# URL de conexión a Redis (dejar vacío si no se usa Redis)
REDIS_URL=""
# Directorio local donde se guardan los archivos subidos por los usuarios
UPLOADS_DIR="uploads"
# Puerto en el que se levanta el servidor HTTP de la aplicación
PORT=4000
