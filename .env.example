# Ejemplo de variables de entorno (NO subir secrets reales)
DATABASE_URL="file:./dev.db"
# Genera un valor seguro, por ejemplo: openssl rand -hex 32
ADMIN_KEY="CHANGE_ME_TO_STRONG_RANDOM_STRING"
REDIS_URL="redis://localhost:6379"
UPLOADS_DIR="uploads"
PORT=4000
